enumerated string non è performante


evoluzione: diversi servizi, un utente può registrarsi ad un servizio specifico.


init non è rest e non andrebbe fatto, meglio all'avvio dell'applicazione

con i requirement non è possibile recuperare gli ordini per sapere gli ID

per i test meglio fare una factory per gli ordini